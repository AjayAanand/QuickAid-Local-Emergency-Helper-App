import React from "react";
import { Text, View } from "react-native";

const SosScreen = () => {
    return(
        <View>
            <Text>
               SOS
            </Text>
        </View>
    );
};
export default SosScreen;